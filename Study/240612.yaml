5회차 조별 스터디 (개인 풀이)

ASCII 코드로 만든 다음 문자열은 메모리에서 몇 바이트를 사용하는가? “good”:
5byte

자바에서 문자는 값이고 문자열은 객체이다. 이 차이가 각각을 연산 또는 사용방식에서 차이를 갖게 만든다. 어떻게 차이가 나는가?:
객체형식은 원본(값) 유지가 가능함, 문자열 객체는 메서드 사용이 가능, 문자는 메서드에 접근이 불가능함.

/모든 객체는 자신이 관리하는 자원을 이용하기 위한 서비스를 제공하고 있다. 따라서 객체지향 프로그래밍에서는 다음과 서비스 객체를 이용하는 방식으로 프로그래밍을 한다.
예를 들어 문자열을 다루려면 문자열 객체의 서비스를 이용하고, 파일을 다루려면 파일 객체의 서비스를 이용하며, 윈도우를 다루려면 윈도우 객체의 서비스를 이용한다.
그렇다면 만약에 여러분이 스피커 음량을 조절하고 싶다면 어떤 객체를 찾아 볼 수 있을까? 직관적으로 답을 쓰시오.:
스피커 음량 조절 객체

객체와 개체에 비유할 수 있는 일상에서의 사전적인 용어를 찾고 그 이유를 설명하시오.:
개체 - 학원    |  하나의 독립된 생물체 (class)
객체 - 뉴렉처  |  일반적 대상

참조변수가 무엇인지를 한 문장으로 설명한다면?:
실제값을 가지고 있는 객체의 주소를 참조하는 변수

가비지 콜렉터의 역할에 대해서 설명하시오:
더이상 참조되지 않는 객체를 메모리에서 정리함


다음은 문자열을 비교하는 연산이다. 그 결과를 쓰시오.:
String name1 = “아이유”;
String name2 = “아이유1”;
String name3 = new String(“아이유1”);
System.out.println(name1 == name2);
System.out.println(name2 == name3);
System.out.println(name1.equals(name2));
System.out.println(name2.equals(name3));
=
false
false
false
true
   
다음 content 문자열에 “<div>”가 포함되어 있는지를 알아보는 코드를 작성하시오.:
String content = “....”;
boolean result = [                                                ]; // content 문자열에 <div>가 있나요?
System.out.println(result); // 출력 결과는 true 또는 false가 되어야 함.
=
content.indexOf(“<div>”) > -1

다음 content 문자열에서 두 번째 div 태그로 감싸인(“<div>”와 “</div>” 사이에 있는) 문자열(“오징어를 반 값에….”)을 출력하는 코드를 작성하시오.(단, 앞/뒤 빈 공백은 제거하시오):
String content = “”“<section>
			<h1>새로운 소식 공지입니다.</h1>
			<div>
				오늘 하루만 특별히….
			</div>
			<div>
				오징어를 반값에….
			</div>
		     </section>”””;
=
int startIndex = content.lastIndexOf(“<div>”);
Int endIndex = content.lastIndexOf(“</div>”);
String s = content.substring(startIndex + “<div>”.length(), endIndex)
                  .trim();	
System.out.print(s);

비밀번호를 저장하고 있는 pwd 변수가 있다. 그 값이 “Hello123”인지를 알아보는 코드를 작성할 것.(단, 소/대문자는 가리지 않는다);:
String pwd = “...”; // 소/대문자 상관없이 “Hello123”과 같아요?
boolean result = [pwd.equalsIgnoreCase(“Hello123”)]; // equals는 소&대문자를 구분한다, IgnoreCase를 추가해줘야 상관없음.  
System.out.println(result); // 출력 결과는 true 또는 false가 되어야 함.

다음 두 객체를 사용하여 프로그램을 만들려고 한다.:
Integer n = new Integer(3);
String str = new String(“hello”);
정수 값을 문자열로 변경하는 기능은 두 객체 중에 하나 또는 모든 객체의 서비스 함수로 존재한다. 서비스 함수는 어떤 객체에 존재할지를 먼저 직관적으로 생각한 답을 쓰시오:
=
Integer

그리고 실제로 그 기능을 찾아서 그 답을 쓰시오.:
int n = 3;
String s = [String.valueOf(n)]; // n 의 값 3을 문자열 “3”으로 변경하는 코드;
System.out.println(s);
=
”3”
