App - Java Platform - OS API - 장치 driver:
자바 플랫폼이 제공하는 환경에서 OS API의 언어를 이용하여, 장치와 연결해준다.

# 콘솔의 문자 입출력
콘솔(키보드 등)의 입력 시, 고유의 Key code가 전달된다.

코드화(기호화): 
어딘가에 기록하기 위해서 필요한 과정

# 각 나라의 언어를 포함하는 Unicode
*UTF-8 - 영어를 사용하기에 적절하여 가장 널리 사용된다. ex) 웹 페이지, 이메일, 텍스트 처리
*UTF-16 - 16비트로 사용하기에 1byte 크기의 영어를 사용하는 환경에서는 선호하지 않고, 다국어 환경에서 선호한다. ex) Windows 시스템, Java 프로그래밍 시스템
바이트 순서 표기(Byte Order Mark): 빅 앤디언 & 리틀 앤디언(intel)
*UTF-32 - 32비트에서만 사용되는 문자가 있을때 사용된다. ex) 이모티콘

# 비트 연산자
ex) 273
*2진법: 0001_0001_0001
*8진법: 0421(3자리씩 묶어서)
*16진법: 111(4자리씩 묶어서)

# 값 형식과 참조 형식
값 형식:
이 형식으로 선언한 변수는 값을 담을 수 있는 공간 생성
참조 형식:
저장하는 버퍼를 참조하는 것, 반드시 new 명령어로 만들어줘야한다.

*HTML

# 블록&인라인 스타일 태그의 차이점
블록 스타일 태그:
문자를 감싸는 형식의 크기가 한줄로 이루어져 있어, 문자 추가시에 다음 줄로 넘어간다.
인라인 스타일 태그:
문자 크기 만큼만 감싸는 형식이기에 다른 컨텐츠를 추가하여도 옆으로 추가가 가능하다.

* 4회차 조별 스터디 (개인 풀이)

ASCII는 미국에서 사용하는 문자를 숫자로 변환하기 위한 코드표이다. 왜 숫자로 변환하는 코드가 필요한가?:
컴퓨터가 문자를 이해하기 위해서는 문자를 숫자로 일대일 대응해서 표현하는 방법이 필요함 

ASCII 외에 한글을 저장하기 위해 EUC-KR 코드표가 있다. 그럼에도 불구하고 유니코드가 필요한 이유는?:
다국적언어를 하나로 표현하기 위해

유니코드 표현 방법 중에서 UTF8을 가장 많이 사용하는 이유는?:
태그나 코드가 영어나 기호로 이루어져있어서 효율성이 좋다

UTF-8, 16, 32 중에 한국어를 사용하기에 가장 적합한 유니코드 표현방식은?:
UTF-16

버퍼(buffer)의 사전적인 의미는 무엇인가?:
데이터를 일시적으로 저장하는 메모리 영역

우리 일상에서 버퍼가 사용되는 예를 두 가지 이상 쓰시오.:
댐, 유튜브

데이터를 디스크에 저장하려면 버퍼를 사용한다. 그 이유는?:
비동기처리를 위해

동기적과 비동기적으로 일한다는 말의 의미를 각각 설명하시오:
동기적 - 한 작업이 완료되기전에 다른 작업을 실행할 수 없는 것
비동기적 - 작업의 완료여부와 관계없이  다른 작업을 실행할 수 있는 것

다음 형식 중에 기본형식이 아닌 것을 모두 고르시오.:
byte, short, int, Integer, long, float, double, char, boolean, String
- String,Integer

기본형식은 값 형식이라고도 한다. 그럼 기본형식이 아닌 형식은 무엇이라고 하는가?:
참조 형식, 클래스 형식

기본형식이 아닌 형식으로는 변수를 선언해도 값을 저장할 공간이 생기지 않는다. 다만 공간을 참조하기 위한 주소공간(주소를 저장하는 공간)만 생긴다. 그렇다면 과거 C++의 포인터와 무슨 차이가 있는지 설명하시오.:
포인터 변수를 생성할 때는 객체가 생성되지 않음
포인터는 주소를 직접 저장할 수 있지만 자바 참조 변수는 주소를 직접 저장할 수 없다.
 
AA aa; // (AA 형식이 있다고 가정) aa는 참조하는 변수 일 뿐 공간이 없다. aa가 참조할 수 있는 공간을 생성하는 코드를 작성하시오.:
aa = new AA(); [                                      ];

파일 출력을 위해 비동기적인 출력을 위해 버퍼를 이용한다. 그 파일 출력 버퍼를 사용하기 위해 자바 플랫폼이 제공하는 출력버퍼의 형식명칭은?:
FileOutputStream

자바가 제공하는 파일출력버퍼 객체를 이용해서 “C:\temp\hello.txt” 파일에 “H”를 저장하려고 한다. class, main 함수를 비롯한 전체 코드를 작성하시오.:
public class App {
  public static void main(String[] args) throws Exception {
	  	FileOutputStream fos = new FileOutputStream( “C:\\temp\\hello.txt”);
		  fos.write(‘H’);
  }
}

